# Intentionally malformed Makefile for pedant e2e testing.
#
# Expected finding:
#   editorconfig  -- Makefile must use tabs (bundled .editorconfig), file uses spaces.

.PHONY: build
build:
    echo "build"
