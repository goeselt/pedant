---
# Intentionally malformed YAML file for pedant e2e testing.
#
# Expected findings:
#   yamllint truthy  -- 'yes' and 'on' are not in allowed-values ['true', 'false']

name: example
enabled: yes
debug: on
