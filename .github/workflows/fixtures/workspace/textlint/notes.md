# Notes

This file mentions the frontend stack, which the workspace `.textlintrc.json` treats as a banned term.

Expected finding with custom config: one terminology hit on the word above. No findings with bundled config (the bundled
textlint config has no such term).
