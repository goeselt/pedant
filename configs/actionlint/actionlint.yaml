---
# Default actionlint configuration.
# Baked into the container image at /etc/pedant/actionlint/actionlint.yaml.
# Projects can override by placing their own .github/actionlint.yaml in the repository.

# Note: runner-label suppression is not configured here.
# The actionlint YAML `ignore:` field has no effect on runner-label errors;
# suppression is applied via the -ignore CLI flag in pedant's tool definition.
